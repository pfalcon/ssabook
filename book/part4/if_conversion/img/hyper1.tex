\documentclass{article}

\usepackage[latin1]{inputenc}
\usepackage{tikz}
\usetikzlibrary{shapes,arrows,positioning}
\usetikzlibrary{calc,backgrounds}

\usetikzlibrary{fit}
\usetikzlibrary{backgrounds}	

\begin{document}
\pagestyle{empty}


% Define block styles
\tikzstyle{decision} = [diamond, draw, fill=blue!20, 
    text width=4.5em, text badly centered, node distance=3cm, inner sep=0pt]
\tikzstyle{block} = [rectangle, draw, fill=blue!20, 
    text width=5em, text centered, rounded corners, minimum height=4em]
\tikzstyle{line} = [draw, -latex']
\tikzstyle{cloud} = [draw, ellipse,fill=red!20, node distance=3cm,
    minimum height=2em]
    
\begin{tikzpicture}[node distance = 1.6 cm]
    \tikzstyle{stateEdgePortion} = [black];
    \tikzstyle{stateEdge} = [stateEdgePortion,->];

    % Place nodes
    \node [rectangle, draw] (bb1) {BB1};
    \node [rectangle, draw, below left of = bb1] (bb2) {BB2};
    \node [rectangle, draw, below right of = bb1] (bb3) {BB3};
    \node [rectangle, draw, below right of = bb2] (bb4) {BB4};
    \node [rectangle, draw, below right of = bb4] (bb5) {BB5};
    \node [rectangle, draw, below left of = bb5] (bb6) {BB6}
    edge [out=300, in=60, looseness=2.6, ->] (bb1);

    % Draw edges
    \path [line] (bb1) -- (bb2);
    \path [line] (bb1) -- (bb3);
    \path [line] (bb2) -- (bb4);
    \path [line] (bb3) -- (bb4);
    \path [line] (bb4) -- (bb5);
    \path [line] (bb5) -- (bb6);
    \path [line] (bb4) -- (bb6);

%    \node [draw=red, inner sep=0pt, ellipse, fit=(bb1) (bb3) (bb4) (bb5) (bb6)]  (a) {};

%    \coordinate (bb1N) at ($(bb1.north) + (0,1em)$);
%    \coordinate (bb4S) at ($(bb4.south) + (0,-1em)$);
%    \coordinate (bb2W) at ($(bb2.west) + (-1em,0)$);
%    \coordinate (bb2WS) at ($(bb2.west) + (-1em,-4em)$);
%    \coordinate (bb2WN) at ($(bb2.west) + (-1em,4em)$);

%    \filldraw [gray] (bb4S) circle (2pt);
%    \filldraw [gray] (bb2W) circle (2pt);
%    \filldraw [gray] (bb2WS) circle (2pt);
%    \filldraw [gray] (bb2WN) circle (2pt);

%    \draw (bb4.south) -- (bb4S);
%    \draw (bb1N) edge[stateEdge] (bb1.north);
%    \draw (bb4S) .. controls (bb2WS) .. (bb2W);
%    \draw (bb2W) .. controls (bb2WN) .. (bb1N);

%     \draw [->] (bb4.south) to [bend right=45] (bb1.north);
\end{tikzpicture}


\end{document}
